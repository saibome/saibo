{
  "install": {
    "command": "corepack enable && pnpm install"
  },
  "build": {
    "command": "corepack enable && pnpm run build"
  },
  "publish": {
    "directory": "dist"
  },
  "assets": {
    "directory": "dist"
  },
  "functions": {
    "enabled": false
  }
}
